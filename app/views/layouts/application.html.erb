<!DOCTYPE html>
<html>
    <head>
        <title><%= title base: :market %></title>
        <meta name="viewport" content="width=device-width, initial-scale=1, user-scalable=no">
        <%= current_stylesheet %>
        <%= csrf_meta_tags %>
    </head>
    <body>
        <%= render 'flash' %>
        <%= render 'header' %>

        <%= yield %>

        <%= current_javascript %>
    </body>
</html>
